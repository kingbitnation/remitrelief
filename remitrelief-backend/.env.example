PORT=4000
HORIZON_URL=https://horizon-testnet.stellar.org
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
NETWORK_PASSPHRASE="Test SDF Network ; September 2015"

# Funded testnet account the backend uses to submit release() calls and pay
# network fees. Create + fund via https://friendbot.stellar.org — NEVER use
# a mainnet secret here.
BACKEND_SIGNER_SECRET=

# Set after deploying the escrow contract (see src/contracts/escrow-contract/DEPLOY.md)
DEMO_ESCROW_CONTRACT_ID=
